<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity"
    android:background="@drawable/dwarf_warrior">

    <ImageView
        android:id="@+id/epLogoIMG"
        android:layout_width="wrap_content"
        android:layout_height="105dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="11dp"
        app:srcCompat="@drawable/nerdr_logo_large" />

    <EditText
        android:id="@+id/liUsernameText"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/rounded_edittext"
        android:drawableRight="@drawable/email_at"
        android:hint="Email"
        android:inputType="textPersonName"
        android:padding="3dp" />

    <EditText
        android:id="@+id/liPasswordText"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/liUsernameText"
        android:layout_margin="15dp"
        android:background="@drawable/rounded_edittext"
        android:drawableRight="@drawable/lock"
        android:hint="Password"
        android:inputType="textPassword"
        android:padding="3dp" />

    <Button
        android:id="@+id/liLoginBtn"
        android:layout_width="250dp"
        android:layout_height="32dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/liPasswordText"
        android:layout_margin="20dp"
        android:background="@color/colorAccent"
        android:text="Login"
        android:textColor="@color/colorWhite"
        android:textSize="18sp"></Button>

    <TextView
        android:id="@+id/liRegistrationLink"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/liLoginBtn"
        android:layout_centerHorizontal="true"
        android:layout_margin="10dp"
        android:text="Register here."
        android:textSize="14sp"
        android:textColor="@color/colorWhite"/>

    <ImageView
        android:id="@+id/liInfoImage"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="10dp"
        android:layout_marginRight="10dp"
        app:srcCompat="@drawable/if_info_white" />
</RelativeLayout>